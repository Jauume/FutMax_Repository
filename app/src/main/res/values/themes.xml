<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Usamos NoActionBar para que NO haya ActionBar predeterminada
         y podamos manejar nuestra propia Toolbar en MainActivity. -->
    <style name="Theme.FutMax2" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">#48C9B0</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white_perla</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- color de fondo o subrayado, por ejemplo para el BottomNavigation -->
        <item name="colorOnSurface">@color/verde_cesped</item>

        <!-- Ejemplo: si prefieres iconos oscuros en la barra de estado -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

</resources>
